import AppCore from 'comptechsoft-app-starter'

const Makers = AppCore.GridMakers

module.exports = [

    // Makers.tlbRefesh(v => v.dt.loading  ? 'fa fa-spinner fa-spin' : 'la la-refresh', 'btn btn-secondary'),
    // Makers.tlbInsert(v => 'la la-plus-circle', 'btn btn-secondary'),
    
]